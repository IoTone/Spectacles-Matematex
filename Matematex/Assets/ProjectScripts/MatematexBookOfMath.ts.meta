- !<AssetImportMetadata/0cf4e1fc-29cf-4dc4-8c5f-1c094d8a0ab2>
  ImportedAssetIds:
    TypeScriptAsset: !<reference> bbc6d1b7-39ac-4507-9457-b992789480c3
  ImporterName: TypeScriptAssetImporter
  PrimaryAsset: !<reference> bbc6d1b7-39ac-4507-9457-b992789480c3
  PackageType: NotAPackage
  LegacyPackagePolicy: ~
  ExtraData:
    {}
  AssetDataMap:
    TypeScriptAsset: !<own> 2e6190f6-4233-4f57-b327-9c0b6175d509
  DependentFiles:
    []
  ImporterSettings: !<AssetImporterSettings>
    {}
  CompressionSettings: !<own> 00000000-0000-0000-0000-000000000000
- !<TypeScriptAssetData/2e6190f6-4233-4f57-b327-9c0b6175d509>
  SvgIcon: ""
  SetupScript:
    code: ""
  Description: ""
  VersionMajor: 0
  VersionMinor: 0
  VersionPatch: 0
  ComponentUid: 6d759369-21da-4f25-bb7e-04367d892bb2
  ExportUid: 00000000-0000-0000-0000-000000000000
  PackagePolicy: CanBeUnpacked
  ScriptInputsHidden:
    aboutButton: false
    autoAdvanceSec: false
    boldFont: false
    calibrationProbe: false
    chapter1Button: false
    chapter2Button: false
    chapter3Button: false
    chapter4Button: false
    containerWorldY: false
    containerWorldZ: false
    cover: false
    displayMode: false
    emToWorld: false
    hideTemplate: false
    italicFont: false
    italicMinGapEm: false
    italicScaleAdjust: false
    layoutWidthMargin: false
    lineMaterial: false
    mainFont: false
    nextButton: false
    pageSheet: false
    pageTurnTrack: false
    prevButton: false
    proofButton: false
    proofOffsetX: false
    proofOffsetY: false
    proofOffsetZ: false
    proofWorldScale: false
    searchButton: false
    searchScreen: false
    showProofs: false
    size1Font: false
    size2Font: false
    soundVolume: false
    soundWindowLength: false
    soundWindows: false
    sqrtWidthScale: false
    templateText: false
    textColor: false
    textScaleMultiplier: false
    titleAccentColor: false
    useWorldSpace: false
  ScriptTypesHidden:
    {}
  ReadMe: !<reference> 00000000-0000-0000-0000-000000000000
  DeclarationFile: !<reference> 00000000-0000-0000-0000-000000000000
  Tags:
    []
  Attachments:
    []
  DefaultScriptInputs:
    - lineMaterial: !<reference.Material> d8596ebf-e771-4a92-9430-31cc2a994424
      templateText: !<reference.SceneObject> 00000000-0000-0000-0000-000000000000
      mainFont: !<reference.Font> 00000000-0000-0000-0000-000000000000
      italicFont: !<reference.Font> 0b51787e-7320-4143-8277-bedeb78dd015
      boldFont: !<reference.Font> 00000000-0000-0000-0000-000000000000
      emToWorld: !<float> 5.000000
      textScaleMultiplier: !<float> 5.000000
      layoutWidthMargin: 1.180000
      sqrtWidthScale: 0.850000
      italicScaleAdjust: !<float> 1.000000
      italicMinGapEm: !<float> 0.000000
      textColor: !<Vec4> {x: 1.000000, y: 1.000000, z: 1.000000, w: 1.000000}
      displayMode: true
      showProofs: true
      proofWorldScale: 1.500000
      proofOffsetX: !<float> 0.000000
      proofOffsetY: !<float> -25.000000
      proofOffsetZ: !<float> -10.000000
      containerWorldZ: !<float> 39.000000
      containerWorldY: !<float> 0.000000
      useWorldSpace: false
      prevButton: !<MappingBased.AssignableType> 00000000-0000-0000-0000-000000000000
      nextButton: !<MappingBased.AssignableType_1> 00000000-0000-0000-0000-000000000000
      chapter1Button: !<MappingBased.AssignableType_2> 00000000-0000-0000-0000-000000000000
      chapter2Button: !<MappingBased.AssignableType_3> 00000000-0000-0000-0000-000000000000
      chapter3Button: !<MappingBased.AssignableType_4> 00000000-0000-0000-0000-000000000000
      chapter4Button: !<MappingBased.AssignableType_5> 00000000-0000-0000-0000-000000000000
      searchButton: !<MappingBased.AssignableType_6> 00000000-0000-0000-0000-000000000000
      aboutButton: !<MappingBased.AssignableType_7> 00000000-0000-0000-0000-000000000000
      proofButton: !<MappingBased.AssignableType_8> 00000000-0000-0000-0000-000000000000
      pageSheet: !<reference.SceneObject> 00000000-0000-0000-0000-000000000000
      size1Font: !<reference.Font> 00000000-0000-0000-0000-000000000000
      size2Font: !<reference.Font> 00000000-0000-0000-0000-000000000000
      cover: !<MappingBased.AssignableType_9> 00000000-0000-0000-0000-000000000000
      pageTurnTrack: !<reference.AudioTrackAsset> 00000000-0000-0000-0000-000000000000
      soundWindows: 10
      soundWindowLength: !<float> 1.000000
      soundVolume: !<float> 1.000000
      titleAccentColor: !<Vec4> {x: 1.000000, y: 0.780000, z: 0.350000, w: 1.000000}
      searchScreen: !<reference.SceneObject> 00000000-0000-0000-0000-000000000000
      hideTemplate: true
      autoAdvanceSec: !<float> 0.000000
      calibrationProbe: false
  ScriptTypes:
    -
      {}
  InputLines:
    - // @input Asset.Material lineMaterial
    - // @input SceneObject templateText
    - "// @input Asset.Font mainFont {\"hint\":\"Upright font for non-italic glyphs — should be KaTeX_Main-Regular. The layout measures every upright glyph (digits, operators, parens) with KaTeX_Main metrics, so drawing them in any other font guarantees collisions like the 2 in '2ab' touching the a. Leave empty only to fall back to whatever font the template carries.\"}"
    - // @input Asset.Font italicFont
    - '// @input Asset.Font boldFont {"hint":"Bold font (e.g., NotoSans-Bold). Used when the walker encounters \\mathbf or \\boldsymbol. Leave empty to fall back to the regular template font."}'
    - '// @input float emToWorld = 5 {"hint":"World units per em — the scale factor between LAYOUT and RENDERING. Kept at 5.0 and the DRAWN glyphs shrunk to match, rather than the reverse: total formula width is the pen span, so this value alone decides how many formulas overflow the display (2 of 88 here, against 11 at 6.327). Does not affect layout fidelity: the conformance harness measures in em, so this cancels out."}'
    - '// @input float textScaleMultiplier = 3.951 {"hint":"SceneObject scale for cloned glyphs. 3.951 = 5.0 / 1.2654, the measured factor by which Text3D over-draws against emToWorld. Changing emToWorld without rescaling this by the same ratio re-breaks glyph spacing."}'
    - '// @input float layoutWidthMargin = 1 {"hint":"Multiplier on fraction width/bar. 1.0 matches KaTeX exactly. It was set to 1.18 in the scene to stretch bars out to glyphs that were drawn 1.265x oversize — with the scale fixed, that compensation is wrong and 1.0 is correct."}'
    - '// @input float sqrtWidthScale = 1 {"hint":"Scale factor for sqrt radical width. 1.0 is correct with the overbar clipping fix; lower only if you need to force a shorter overbar."}'
    - "// @input float italicScaleAdjust = 0.7957 {\"hint\":\"Uniform scale for italic glyphs, on top of textScaleMultiplier. 0.7957 = 935/1175, the ratio of the two fonts' hhea ascent-descent spans — Lens Studio fits that span to the requested size, so KaTeX_Math-Italic draws 1.2567x larger than KaTeX_Main at the same setting. Not a fudge factor: it is read straight out of the font headers.\"}"
    - '// @input float italicMinGapEm {"hint":"Extra gap (em) forced after every italic glyph. A RENDERING compensator with no basis in TeX layout — 0 is layout-correct. Raise toward 0.12 only as a stopgap if italic glyphs collide; prefer fixing italicScaleAdjust, which addresses the cause instead of padding around it."}'
    - "// @input vec4 textColor = {1,1,1,1}"
    - '// @input bool displayMode = true {"hint":"KaTeX displayMode (textbook style). Big operators get above/below limits, fractions and sqrts render larger. Recommended: ON."}'
    - '// @input bool showProofs = true {"hint":"Render visual proofs alongside formulas that have one (currently: Pythagorean Theorem #1). Toggle off to hide."}'
    - '// @input float proofWorldScale = 1.5 {"hint":"Multiplier from proof units to world units. ~1.5 places a 3-4-5 triangle in a comfortable ~15-unit footprint."}'
    - '// @input float proofOffsetX {"hint":"Local X offset for the proof relative to the formula container."}'
    - '// @input float proofOffsetY = -25 {"hint":"Local Y offset for the proof relative to the formula container. Negative places it below the formula."}'
    - '// @input float proofOffsetZ = 0.5 {"hint":"Local Z offset for the proof relative to the formula container. Must stay IN FRONT of the page (MatematexPage.pageZ, −0.6) — a proof is printed on the page, not floating behind it."}'
    - '// @input float containerWorldZ = -100 {"hint":"Local Z offset (negative = in front of parent). For device: parent this SceneObject to Camera Object."}'
    - '// @input float containerWorldY {"hint":"Local Y offset (height relative to parent)"}'
    - '// @input bool useWorldSpace {"hint":"Use world-space positioning (legacy). Leave OFF for on-device / head-tracked content."}'
    - '// @input AssignableType prevButton {"hint":"PinchButton — previous formula"}'
    - '// @input AssignableType_1 nextButton {"hint":"PinchButton — next formula"}'
    - '// @input AssignableType_2 chapter1Button {"hint":"PinchButton on the splash/TOC page — jump to Chapter 1 Geometry (formula #1)"}'
    - '// @input AssignableType_3 chapter2Button {"hint":"PinchButton on the splash/TOC page — jump to Chapter 2 Algebra (formula #21)"}'
    - '// @input AssignableType_4 chapter3Button {"hint":"PinchButton on the splash/TOC page — jump to Chapter 3 Calculus (formula #41)"}'
    - '// @input AssignableType_5 chapter4Button {"hint":"PinchButton on the splash/TOC page — jump to Chapter 4 Linear Algebra (formula #61). Optional — leave empty if not added to scene yet."}'
    - '// @input AssignableType_6 searchButton {"hint":"PinchButton — open the Search screen. Optional."}'
    - '// @input AssignableType_7 aboutButton {"hint":"PinchButton — toggle the About page (sources, licences, credits). Pinching it again returns to the splash. Optional."}'
    - '// @input AssignableType_8 proofButton {"hint":"PinchButton — show the visual proof for the current formula, full size. Only appears on formulas that have one. Pinching it again returns to the formula. Optional."}'
    - '// @input SceneObject pageSheet {"hint":"The MatematexPage SceneObject that draws the grid-paper sheet. Hidden on the cover."}'
    - '// @input Asset.Font size1Font {"hint":"KaTeX_Size1-Regular — small operators and the smaller scaled delimiters. Also the only shipped face carrying U+2016, which stretchy norm bars are built from."}'
    - '// @input Asset.Font size2Font {"hint":"KaTeX_Size2-Regular — display-size large operators. Without it \\int is laid out 2.22em tall and drawn 0.89em, and \\sum has no glyph at all."}'
    - '// @input AssignableType_9 cover {"hint":"MatematexCover component — the closed book the lens opens on. Optional: without it the lens opens on the half title instead."}'
    - '// @input Asset.AudioTrackAsset pageTurnTrack {"hint":"The page-turn recording. Optional — without it, turns are silent. The AudioComponent that plays it is created in script, so there is nothing else to wire."}'
    - '// @input int soundWindows = "10" {"hint":"How many one-second windows to pick from in the recording. The source is 11.3s of repeated page turns, so each window is a different turn and no two consecutive pages sound identical."}'
    - '// @input float soundWindowLength = "1.0" {"hint":"Seconds of audio played per turn."}'
    - '// @input float soundVolume = "1.0" {"hint":"Playback volume for the turn."}'
    - "// @input vec4 titleAccentColor = \"{1.00, 0.78, 0.35, 1.00}\" {\"hint\":\"Colour of the XR superscript on the title page. Gold by default — the title page carries no proof, so this is free to be an accent rather than one of the proof palette's four meaning-bearing hues.\"}"
    - '// @input SceneObject searchScreen {"hint":"SceneObject hosting the MatematexSearchScreen component. Optional — search is unavailable if empty."}'
    - '// @input bool hideTemplate = true {"hint":"Hide template after cloning"}'
    - '// @input float autoAdvanceSec {"hint":"Auto-advance interval in seconds (0 = disabled). Useful for testing without buttons."}'
    - '// @input bool calibrationProbe {"hint":"Log the first few glyphs of each formula with their metric advance vs their drawn width. Use when glyphs collide or drift: a ratio far from 1.0 means the layout and the renderer disagree about how big an em is."}'
